#include "main.h"
#include <stdio.h>

/**
 *main - this us the function name
 *@argc: parameter to count argument
 *@argv: parameter to index argument in array
 *
 *Return: return 0.
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
