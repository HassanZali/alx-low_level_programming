#include "main.h"

/**
 *string_toupper - function main
 *@n: string input
 *@a: parameter one
 *@z: parameter two
 *Return: dest
 */

char *string_toupper(char *)
{
int i;
i = 0;

	while (n[i] != '\0')
{
	if (n[i] >= 'a' && n[i] <= 'z')

	n[i] = n[i] - 32;
	i++;
}
return (n);
}
